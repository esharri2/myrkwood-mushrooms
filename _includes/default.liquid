<!DOCTYPE html>
<html lang="en" class="font-body text-dark bg-light overscroll-contain">
    {% render 'head', site: site %}
    <body
        class="relative">
        {% render 'skip-to-content' %}
        {% render 'ie-notice' %}
        <header class="bg-accent-1 fixed top-0 right-0 left-0 z-20">
            <div
                class="flex justify-center items-center flex-wrap">
                {% render 'navigation', navigation: navigation, page: page %}
            </div>
            <a aria-label="Myrkwood Mushrooms Instagram" class="fixed right-2 top-2 hidden md:block" target="_blank" rel="noreferrer" href="https://www.instagram.com/barbaric_botany/">{% render 'icons', type: "instagram" %}</a>
        </header>
        <main
            class="min-h-screen"
            id="main">
            {{ content }}
        </main>
        {% render 'footer', title: site.title, builtBy: site.builtBy, navigation: navigation_footer %}
    </body>
</html>
